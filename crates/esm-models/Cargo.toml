[package]
name = "esm-models"
description = "Domain entities, value objects, shared traits, and validation logic"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
