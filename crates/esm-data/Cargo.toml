[package]
name = "esm-data"
description = "JSON schema validation, data pack loading, and mod support infrastructure"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
esm-models = { path = "../esm-models" }
rand = "0.10.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3.26.0"
